program calculaator;


program addition;
var
  a, b, sum: integer;
begin
  a := 1000;
  b := 20;
  sum := a + b;
  writeln('Sum of ', a, ' and ', b, ' is ', sum);
end.

program substraction;
var
  a, b, diff: integer;
begin
  a := 1000;
  b := 20;
  diff := a - b;
  writeln('Difference of ', a, ' and ', b, ' is ', diff);
end.
